<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SE1705" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

<!--        apply a specific style for all buttons in app -->
<!--        <item name="materialButtonStyle">@style/myButton</item>-->
    </style>

    <style name="Theme.SE1705" parent="Base.Theme.SE1705" />
    <style name="myButton">
        <item name="backgroundTint">@color/black</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:radius">20dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center</item>
    </style>
</resources>